%p#notice= notice

%b Title:
= @board.title
= @board.turn
%br
.board
  - (1..19).each do |y|
    .row
      - @board.peices.select {|r| r.y == y}.each do |p|
        .peice
          - if not p.colour
            .empty{:color => @board.turn}= link_to raw("&nbsp;"), board_peice_path(@board, p, :colour => @board.turn), :method => :put
          - else
            %div{:class => p.colour}

.snapple
  hello
= link_to 'Edit', edit_board_path(@board) |
= link_to 'Back', boards_path
